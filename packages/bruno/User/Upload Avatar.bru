meta {
  name: Upload Avatar
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/users/me/avatar
  body: multipartForm
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:multipart-form {
  avatar: @file()
}
