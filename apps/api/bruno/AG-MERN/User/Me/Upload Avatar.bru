meta {
  name: Upload Avatar
  type: http
  seq: 9
}

post {
  url: {{baseUrl}}/users/me/avatar
  body: multipartForm
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:multipart-form {
  avatar: @file(C:\path\to\image.jpg)
}
