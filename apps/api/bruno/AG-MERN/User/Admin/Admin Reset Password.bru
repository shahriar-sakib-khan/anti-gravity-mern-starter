meta {
  name: Admin Reset Password
  type: http
  seq: 6
}

patch {
  url: {{baseUrl}}/users/1/password
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "newPassword": "NewPassword123!"
  }
}
